ContentEditable(true);

const User = _user.Data
  
var UserInput = NewInput(); 

  var userdata = NewUser();
    
Onclick(Submit(UserInput));

 OnCommand(Update(UserData));

 UserInput(Edit(UserData));

// Needs Work 
